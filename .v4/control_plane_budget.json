{
  "ceiling": 15524,
  "why": "2026-09-04,第七十四次:15512 → 15524(+12),kernel/cli.py 三處、kernel/ledger.py 一處、kernel/risk.py 一個新 function。買嘅係四句輸出唔再答一個內部狀態而講另一件事。呢個 repo 自己嘅規則:「工具跑過了」同「閘通過了」係兩個命名空間,絕不合併 —— 四句每句都合併咗一對。\\n\\n一:N/M terminal 數嘅係 len(rows)-len(blocked),而 report kind 嘅 OPEN claim 唔喺 blocked 入面,所以一條 OPEN claim 被數做 terminal。呢個唔係推論 —— 我自己 Wave 2 個 ship 就印咗 14/14 terminal,而同一頁列住一條 OPEN。而家印兩個數,JSON 亦分開兩個 key(terminal 用 state.TERMINAL,not_blocking 係另一件事)。\\n\\n二:v4 review close 印完個條件就 exit 0,而 claim 仲係 OPEN,判詞要另一個指令。Adopter 閂 35 條 finding 嗰陣讀咗 exit 0 加嗰兩行做成功,靠數 status 入面嘅 OPEN 先發現。Exit code 冇改(offer 的確寫咗,呢個係佢個職責),加咗一句 NOT VERIFIED YET 連埋跑得嘅下一步。\\n\\n三:risk accept --no-tty-check 印 signed by <email>,而寫入去嘅 record 係 signed_by: agent、stdin_was_a_tty: false。個檔一直誠實,得個 terminal 唔誠實,而嗰度先係人睇嘅地方。抽咗做 kernel/risk.py::attribution,理由唔止係可測:一句寫喺 entry surface 嘅判斷係一句冇 test 到得到嘅判斷。\\n\\n四:v4 audit 喺第一次 ship 之前報 chain: BROKEN,因為 chain_head.json 未寫 —— 一個新 adopter 第一件事就係行呢個。而家分開兩句:一條 attempt 都冇就講「未有嘢錨,第一次 ship 會寫」,有 attempt 而冇錨就照舊講「一條被剪過嘅 ledger 會驗得乾淨」。兩個都仍然係 problem、都仍然 exit 1 —— 換一句說話,唔係降級一個閘,有第三條 test 釘住兩邊都唔過。\\n\\n順帶還咗自己一筆債:Wave 2 ship 嗰陣 test-shape 報咗一條讀 checker 源碼嘅 test,而嗰刀已經出咗貨。今次改成真跑三個 checker 收唔收 --emit-baseline(argparse 對唔認得嘅 flag exit 2,所以斷言個 exit code)。同一件事喺新檔做多次:五條讀源碼嘅 test 改咗三條做真跑,剩兩條入 baseline 連理由。\\n\\n三個 doc gap 同一刀:.claude/commands/run.md 同 wave.md 講明 engage 一句要提到 scope 入面嘅路徑(adopter 41 句入面 6 句第一次被拒);docs/USING.md 兩段 —— sub-agent cd 去第二個 repo 唔會換走判緊佢嗰個 repo(實測 526 個 hook_seen 行落錯 ledger、worktree 嗰邊零行、寫入靜靜咁放行),同埋目錄 glob 谷大 claim 嘅實測數(58 條對 9 條)。\\n\\n紅步驟:cli 反轉 4 紅、ledger 反轉 1 紅,還原之後 13 條全綠。\\n\\n───\\n\\n2026-09-04,第七十三次:15496 → 15512(+16),分三處:kernel/analysis/shell_command.py、kernel/doctor.py、kernel/runner.py 加 kernel/cli.py。買嘅係三個機制講得出佢哋睇唔到乜。三個都由 adopter 個 VIBEPROOF_LOG 報上嚟,三個都同一個形狀:有嘢答咗,個答案錯或者到唔到,而個答案本身冇講。\\n\\nC:_spellings 把一個已經 tokenize 好嘅 argv word 再按空白拆開,所以英文字 checkers 拎去 match glob checkers/** 會中。實測 git commit -m 'rework the checkers and detectors story' → DENY(cannot tell),同一句換走嗰兩個字 → ALLOW。個 word 入面冇路徑、冇 heredoc、命令 parse 得到。改成:一個由更長嘅 word 入面切出嚟嘅 chunk 要帶分隔符先算路徑;一個冇空白嘅 word 唔會被切,所以 rm -rf checkers 照舊捉到。docstring 列嗰三個來源全部保住,因為三個都係真路徑而真路徑有分隔符。\\n\\n代價講出嚟,唔留畀人日後撞:一個本身冇分隔符嘅 protected glob(Makefile 咁樣)藏喺引號包住嘅 sub-command 入面會漏。本 repo 同 reference adopter 兩邊夾埋八條 glob 全部係 something/**,我逐條數過。呢個漏寫咗做一條可執行斷言(test_what_this_gives_up_written_as_an_assertion),唔係寫成一句承諾。\\n\\nD:doctor 個 facts 行只驗 seen_at。一條 match 唔到任何 tracked file 嘅 glob 做嘅嘢多過收窄一次掃描 —— derive 會直接 refuse 個 detector(facts-narrowed),而嗰個 kind 喺任何 lane 都唔會提出。Adopter 實例:四條由 sibling repo 搬過嚟嘅 ui_globs 喺 port 之後 match 唔到嘢,external_write / runtime_proof / surface_proof 每次 derive 都 refuse,九條 lane 冇一條有 external-write claim —— 包括寫 object storage、換 OAuth token、send 去 chat API 嗰三條。v4 ship 有印 NOT RUN;而 doctor,一個職責就係分開真接線同得個樣嘅指令,印 ok facts。而家 import derive 自己嗰個 _filters_matching_nothing,唔係第二個實作 —— 同一條 glob 由兩個 matcher 判,就係一個 command 話啱一個話錯。\\n\\nE:v4 derive 印「a delta kind takes --emit-baseline」,而 v4 run-checker 只收五個 flag,遞唔到入去。三個 checker(structural_lint、external_write、fail_closed)本身收,所以嗰句話唔係錯,係到唔到。Adopter 搵唔到,改為簽一個 baseline_raise risk,而 baseline 檔留空,下一個掂同一個檔嘅 task 會被問多次。個 flag 由 cli 經 runner.run_checker 傳落 argv;kernel 唔解讀佢印乜,否則 baseline 格式就有兩個 owner。\\n\\n紅步驟三次,逐個修分開反轉:C 反轉 2 紅、D 反轉 2 紅、E 反轉 1 error,三個還原之後 17 條新 test 全綠。成套 suite 過晒(1,7xx 條),即係冇任何既有 test 靠緊舊嗰個 _spellings 行為。\\n\\n───\\n\\n2026-09-04,第七十二次:15482 → 15496(+14),kernel/redgreen.py 立一個 owner、kernel/review.py 同 checkers/review_finding.py 兩處改成問佢,加 kernel/analysis/external_write.py 攞走一個字。買嘅係兩個 checker 唔再講一句關於 code 嘅假話。\\n\\n第一個由 adopter 個 VIBEPROOF_LOG 報上嚟,而佢報嘅因由係錯嘅,呢點值得記低。佢寫「red-green tracer 係 Python-only,所以 TypeScript closing test 永遠 close 唔到」,結論啱,而佢引嘅係個 checker 自己嗰句訊息當證據。實情係 redgreen 喺 2026-08-28(第六十二次)已經加咗 Go 同 Node,而 verify 行嘅正正係嗰條三語言路;過唔到嘅係佢前面嗰道 endswith('.py') 閘,同埋 kernel/review.py:75 一模一樣嗰個判斷。一條規則活得長過佢嘅理由,而佢個理由仲印緊出嚟畀人睇。\\n\\n實測先於落刀,而我兩個預期都錯咗,兩次都冇改預期去遷就結果。第一個預期:Vitest 跑一條真 .ts test 應該 executed=True。觀察:None。第二個預期:有資料但 url 拼法唔同。觀察:306 個 script 記錄咗,一個都唔係嗰個 repo 自己嘅 source。第三次先量到真相 —— --pool=threads 記得到(dashboardRegistry.ts 出現,tier_of executed=True calls=37),而預設 pool 同 --pool=forks 記唔到。所以個閘要開,而開之前要先修一樣嘢。\\n\\n嗰樣嘢係第三個缺陷,冇人報過:review_finding 對 res.ok 為假一律 exit 1,包括 symbol_executed is None。redgreen 自己個註釋為呢個分別寫咗成段 ——「Unknown is not proof. It is just not an allegation」—— 而佢唯一嘅讀者就把佢變成一句指控。Python 之下呢個分別唔使錢(一個跑得起嘅 Python 指令一定留低 trace file),但一放行 .ts,預設 pool 之下今日誠實嘅 exit 4 就會變成「has not earned the right to close this finding」。所以佢係放行嘅前置條件。個新分支讀三個 field 唔係一個:紅綠都成立而淨係追蹤唔到先算 UNSUPPORTED,一條喺 HEAD 過唔到嘅 closing test 照樣 FAIL,對照 test 釘住呢一半。\\n\\n第二個缺陷:nonce 喺 IDEMPOTENCY_WORDS 入面,而佢係嗰堆字入面唯一一個意思相反嘅。Idempotency key 重試時必須一樣(SHAPE_FRESH_KEY 就係報「每次新鮮」呢個缺陷),cryptographic nonce 每次必須唔同。Adopter 實例:meta_oauth.py 個 OAuth state nonce 用 secrets.token_urlsafe,被報做兩個 function 以外嗰個 code-exchange POST 嘅「new-per-call idempotency key」,而嗰個 exchange 由 vendor 契約保證單次使用、從來唔重試。呢個唔係擦邊球:每個 OAuth 或者 webhook 簽名 module 都有一個,所以佢會喺全部嗰啲檔上面報。\\n\\n同一個 repo 入面 kernel/analysis/webhook_replay.py 由另一邊得出過同一個結論並寫低咗 —— 一個「merely *named* nonce」嘅參數係佢其中一個 bypass fixture,因為「a name is not a guard」。external_write 完全靠個名判,所以佢先係要輸掉呢個字嗰個。冇失去嘅覆蓋面:idempotency_nonce 仲有 idempotency 呢個字、按職責命名嗰啲(idempotency_key / dedupe_key / Idempotency-Key)由 IDEMPOTENCY_NAMES 接住、而 SHAPE_RETRY_NO_KEY 本身就係按結構問一個 retry loop 攞 key,從來唔靠詞彙。\\n\\n紅綠四次,全部真做過:攞走 nonce 個新 green fixture(a_csrf_nonce_is_not_an_idempotency_key.py)喺舊 code 之下 want 0 got 1、checker NOT registrable,還原之後 44 個 case 全過;checker 兩處改返舊樣 3 條紅;kernel 兩個檔 stash 走 4 紅 4 error;三樣還原之後 13 條新 test 全綠。review_finding 重新 register:14/14 as expected,sha ecf4e80fda94f9b7。\\n\\n───\\n\\n2026-09-02,第七十一次:15415 → 15482(+67),kernel/ledger.py 為主,加 kernel/sweep.py 同 kernel/accept.py。買嘅係匯出檔分段封存 —— social_ops 嘅 .v4/ledger_export.jsonl 實測 58.74 MB、日增 2.34 MB,十七日到 GitHub 每檔 100 MiB 拒收線。一個檔唔可以無限長,所以 export_jsonl 超過 SEAL_AT 就將開檔原樣改名為 ledger_export.jsonl.0001 永不再寫,新開檔第一行 _segment 記各表已封存行數同鏈頭 hash,verify_exported 跨段走鏈,缺段、截尾、重建都抓到。揀封存唔揀 gzip:append-only 純文字喺 git pack 只佔新增嗰啲行,gzip 每次 ship 係一個全新 blob。真數據 dry run:58.74 MB 封存後新開檔 26 行,跨段驗證 0 問題,5,789 attempt 同 live ledger 一致。順帶 kernel/accept.py 嘅 PYTHONPATH 加返框架路徑(+2),adopter 先跑得到 accept。紅綠:tests/test_an_export_that_outgrows_one_file.py 13 條新,加原本釘住匯出嘅 71 條照綠。\\n\\n───\\n\\n2026-09-02,第七十次:15393 → 15415(+22),全部喺 kernel/spec_coverage.py。買嘅係 docs/SPEC.md 同 docs/README.md 譯成英文之後,啲數字仲有人結算。\\n\\n第六十九次買咗三個 population alias,呢次係同一條路行埋落去 —— 而分別喺於,呢次係喺譯緊嗰一刻逐格對住個 checker 試出嚟,唔係事後補。§13.5 張表六個數,四個原本靠中文量詞先結算得到:21 個 kind、21 個 checker、13 個 lens、303 條 check。譯成 21 claim kinds / 21 registered checkers / 13 reviewer lenses / 303 reviewer checks,四個全部照舊結算;譯成 21 kinds / 303 checks 就四個全部靜音,而 v4 accept 照樣綠。docs/README.md 個 pin 數同一件事,所以有 design pin。\\n\\n一個設計決定要記低,因為佢係我第一版寫錯咗然後量出嚟嘅:一個 key 嘅第一個字就係令一個冇量詞嘅數變得可讀嗰樣(_names_its_population 讀 narrowing),所以嗰個字必須係一個修飾詞,唔可以係一個 population 自己。我第一版寫 lens check 同 detector file,即刻多咗九條 finding,而入面四條係假嘅:docs/SPEC.md 兩處 `11 lens(es), 214 finding(s)` 係示範 CLI 輸出,`3,502 detector runs` 個千分位令個 regex 讀成 502。所以改成 reviewer check,而 detector 嗰個唔要 —— detectors/ 20 個檔嘅總數而家冇人結算,而佢兩半(11 conditional + 9 always_*)兩邊都有人結算,加埋就係嗰個總數。\\n\\n順帶買咗 doctrine rule:§13.5 張表有一行講層 ① 有幾多條,佢寫住 88,而 kernel/doctrine.py::DOCTRINE 有 96 —— 正正就係嗰張表自己嗰句警告講緊嘅漂移,出喺唯一一格冇人讀得到嘅數。由 root 嗰個檔 literal_eval 出嚟,唔係 import 呢個 process 自己嘅 kernel.doctrine,因為一個 checker 可以對住另一棵樹跑。加咗之後即刻捉到 docs/EVIDENCE.md:160(34 of 88 doctrine rules),已標做講過去。\\n\\n紅綠:拆走四個 alias 同 _doctrine_rules,新嗰班五條紅,原本嗰 23 條照綠;還原之後 28 條全綠。另外揸住一條會失敗嘅 README 數字實測過,確認個英文寫法真係入到 counted_claims,唔係靜靜跳過。\\n\\n───\\n\\n2026-09-02,第六十九次:15390 → 15393(+3),喺 kernel/spec_coverage.py::_reality。三個英文 population alias:engaged kind、engagement rule、main-only checker。\\n\\n呢個係同一類嘅第三處,亦係我同 repo 擁有者講過「唔存在」嗰一處。我當時話 counted_claims 已經讀得英文,證據係 8 auth_decision rows settle 得到 —— 對通用路徑啱,對 repo-specific 嗰條路錯:_reality 有兩個 population 嘅 key 本身寫成中文詞組,而一個 key 就係一句散文要用邊個詞先結算得到。實測:7 個判斷唔喺 entry file 嘅 checker settled=True,同一句英文 settled=False,即係譯完 docs/SPEC.md 之後嗰個數冇人結算。\\n\\n用 main-only checker 而唔用 thin:SPEC §4 嗰段本身就係講 thin 有四個重現得到嘅答案,而一個 key 係文件必須寫出嚟嘅詞,所以佢唔可以係嗰個被放棄嘅字。用別名而唔係取代:一個譯到一半嘅 repo 兩種寫法都有,而呢個 repo 就係。\\n\\n驗過冇放寬:7 main-only checkers、10 engaged kinds、29 engagement rules 三句英文全部 settle,中文照樣 settle,而 27 checkers(冇講明 population)照舊唔 settle。\\n\\n加咗之後第一次跑就捉到一條:docs/EVIDENCE.md:51 講 17 engaged kinds 而今日係 10。嗰行係啱嘅 —— 佢描述 X/Y 實驗當時嘅框架配置 —— 而由頭到尾冇任何嘢問過佢,因為 population key 係中文而份文件係英文。已標做講過去。1,704 條全綠。\\n\\n───\\n\\n2026-09-01,第六十八次:15386 → 15390(+4),喺 kernel/spec_coverage.py。買嘅係餘下兩個中文錨點讀得英文。呢兩個唔係文法,係認住文件入面一個地標,而一個搵唔到自己地標嘅檢查係乜都唔報而報成功。\\n\\nlayer_one_has_what_it_was_assigned:landmark 由 層 ① 擴到認埋 layer 1 同 layer 4 cannot,而引號亦要換 —— 「」係中日韓標點,英文用 backtick。抽取方式唔同係因為語言唔同:「唔好用 stopgap」剝走 唔好 同 用 剩返一個技術詞,而英文 do not use a stopgap 由頭到尾都係 ASCII,照剝就會攞成句去 doctrine 度查。所以英文只接受單一個字嘅 backtick(fullmatch [A-Za-z][A-Za-z_-]*),kernel/doctrine.py 呢種路徑唔算 —— 否則每個出現喺 layer-1 行嘅 symbol 都變成 doctrine 要交代嘅嘢。\\n\\nPAST 收埋三處寫死嘅標記,淨值零。順帶揾到一件事:當時/前身 三處都有,而 V3 同 rev 1 得兩處有,第三處冇 —— 兩種寫法對每個輸入出一樣答案,所以冇 test 見到,呢個就係一條冇 owner 嘅規則喺入面睇落嘅樣。\\n\\nuser_facing_docs 本來喺張 list 上面而冇改:查完之後佢唯一嗰句中文喺 docstring,個 body 一個字都冇讀。\\n\\n紅綠:同時拆走 LAYER_ONE 個英文半邊同 PAST 四個英文標記,兩條紅,中文七條照綠 —— 包括對住本 repo 真 SPEC 掃一次要 0 條。1,704 條全綠。\\n\\n───\\n\\n2026-09-01,第六十七次:15373 → 15386(+13),全部喺 kernel/spec_coverage.py。買嘅係條 lead-in 數量檢查讀得英文。\\n\\n點解要買:呢啲文件要譯做英文,而實測同一個錯 —— lead-in 講四而底下 block 得三 —— 中文捉到,英文句子同英文標題兩種形式都返 []。docs/SPEC.md 一份就有 343 個 lead-in 形狀,即係譯完之後嗰 343 個檢查點全部變零,而 v4 accept 照印同一個綠。\\n\\n量度亦改正咗我自己一個講法。我原本同 repo 擁有者講「559 處會靜音」,把 lead_in_findings 同 counted_claims 加埋計。錯:counted_claims 個數字本身係 Arabic digits,而佢已經有一條量過嘅英文路徑 —— _names_its_population,當年直接拆走量詞喺呢個 repo 自己啲英文 docs 出 50 條假 finding,加返呢條規則變 0。實測 8 auth_decision rows settled=True。所以真正歸零嘅係 lead_in_findings 嗰 343 個。\\n\\n一個設計分歧要記低:LEAD_IN_REACH 係 14 個字元,喺中文一個詞 1-2 字啱用,喺英文係兩三個詞,會拒絕 four colours the gate enforces: 呢種講緊底下 block 嘅 lead-in。所以英文嗰半數詞唔數字元,六個詞,而兩條 fixture 分別坐喺兩邊:離冒號三個詞要捉,十個詞唔捉。\\n\\n個 PAST 豁免亦跟住走:當時 同 前身 兩個都係中文,一份譯咗嘅文件會開始報自己啲歷史係數錯。\\n\\n紅綠:拆走英文分支,六條紅;中文嗰班五條(包括 ordinal 第三層、冠詞 一個)兩邊都綠。個新 pattern 第一次跑就喺 docs/EVIDENCE.md:170 紅咗一條真嘢 —— 個標題 Three routes compared 係啱嘅,而底下 fenced block 第一行係欄名,_block_size 分唔開表頭同資料,所以加咗 count-exempt 連理由。1,695 條全綠。\\n\\n───\\n\\n2026-09-01,第六十六次:15372 → 15373(+1),喺 kernel/doctor.py。一句讀 claim_kinds.json,買嘅係條 unanswerable kinds line 唔再報一個 registry 已經冇咗嘅 kind。\\n\\n實測喺呢個 repo:嗰條 line 講 dependency、request-coverage、secret-chain 三個 blocked,而三個都唔喺 claim_kinds.json 入面 —— 冇嘢提得起佢哋,所以嗰啲 exit 4 唔係一個未還嘅義務,係一條呢個 repo 決定唔要嘅規則嘅紀錄。而佢印嘅解決方法(v4 risk accept --kind unprovable --scope repo)指住一個唔存在嘅 kind。\\n\\n呢個係同一條 line 第三次修。頭兩次 —— 改成列出 task 名而唔係一個數,同埋標記邊個 task 已經 abandoned —— 都係令一份講緊死歷史嘅報告易讀啲,冇一次移走過死歷史本身。\\n\\n紅綠做過:拆走個 filter,三條紅(兩條合成 fixture,加一條對住本 repo 現況);而第四條 test_and_a_kind_that_is_still_registered_is 兩邊都綠,佢守住呢個 filter 唔係一個關掣。個紅步驟亦揭發咗 test_kernel.py 兩條既有 test 靠一個空 registry 嘅 fixture,已修。1,679 條全綠。\\n\\n───\\n\\n2026-09-01,第六十五次:15354 → 15372(+18),全部喺 kernel/analysis/fail_closed.py。買嘅係兩個由 adopter 實戰揾返嚟嘅缺口。\\n\\n第一個:ERROR_RESULT_WORDS 係一張逐個詞形手寫嘅表,而一張逐個詞形手寫嘅表要列齊每個概念嘅每個變化。佢冇:有 failed 冇 fail、有 denied 冇 deny、有 refuse 冇 refused,而 reject/rejected 啱好兩個都有。實證係 adopter 改咗個 helper 名,兩條 finding 由 FAIL 變 PASS,一行邏輯都冇改。改成 stem 加 startswith。代價係 failsafe 會讀成 fail —— 而呢個代價喺豁免嗰邊,一條豁免多咗一個 handler,平過一條收埋咗四個。實測 _abort 同 _bail 仍然唔認:佢哋唔喺原本嗰四個概念入面,加佢哋係另一個決定。\\n\\n第二個,而佢一半係被呢個 repo 自己嘅 fixture 改正嘅。報上嚟嘅缺口係:return 一個 verdict dict 算豁免,裸 return False 唔算,所以一個 -> bool 而 False 本身就係判詞嘅 helper 永遠被判 falsey —— 對方為咗呢個 baseline 咗五條。第一版寫成「-> bool 加 return False 就豁免」,而註冊閘即刻 REFUSED,一次過令三個 bypass fixture 由 exit 1 變 exit 0。三個都係 verify_signature(request, secret_store) -> bool 配 except Exception,而 bypass 嘅定義就係一個必須繼續被報嘅形狀。佢哋啱:一個 catch-all 連「secret store 冧咗」都捉埋,所以嗰個 False 係喺報告一個從來冇跑過嘅比較,而 caller 分唔開佢同一個真嘅唔匹配。所以個豁免要求個 handler 捉住一個具名例外。六個邊界逐個實測:具名例外豁免,except Exception、bare except、tuple 入面有 catch-all、-> str、return None 五個照樣報。\\n\\n兩個新 fixture,cases 47 → 49 as expected。1,675 條 test 全綠。\\n\\n───\\n\\n2026-09-01,第六十四次:15339 → 15354(+15),全部喺 kernel/layout.py::declared_repo_name。買嘅係「呢個 repo 叫咩名」由檔案系統推導,改成由 repo 自己宣告。\\n\\n呢一類失敗係第二次,而第二次就係第一次修錯咗位嘅證據。第一次係 worktree:一個喺 …/wt-d1 嘅 worktree 跑 v4 doctrine(佢冇 flag 一樣會寫檔),把 CLAUDE.md 個標題改成 wt-d1,之後每個 worktree 嘅 registry-consistency 都紅;一個行八個 worktree 嘅 adopter 嘅做法係把八個目錄全部改名。當時個修法係問 git rev-parse --git-common-dir 攞 main worktree 個目錄名。\\n\\n第二次係今日,喺已經 push 咗嘅公開版度量到:由 GitHub clone 落一個叫 verify-public 嘅目錄跑 v4 accept,registry-consistency 紅,訊息同上次逐字一樣 —— CLAUDE.md is not what v4 doctrine generates —— 因為個標題 render 緊目錄名。git 個 common dir 個 parent 仍然係一個目錄名,所以 git clone <url> my-name 同 GitHub 自己個 Download ZIP(解出嚟叫 <repo>-main)兩樣都行得穿。\\n\\n所以個推導由頭到尾都唔係修復。一個寫咗 facts 表嘅 repo 已經講咗自己叫咩,而嗰個宣告跟住 bytes 走。新次序:宣告、git、目錄名。兩張表嗰陣唔讀 —— 因為 config.facts_path_for 正正要靠呢個答案去揀邊張,讀任何一張都係循環。\\n\\nRed-green:拆走 repo_name 頭兩行,DeclaredNameWins 三條紅、FilesystemAnswersWhenNothingIsDeclared 六條照綠;還原之後 11 條全綠。個紅步驟順手揭發咗一條我自己寫嘅假 test —— doctrine 嗰條原本斷言 splitlines()[0],而嗰行係 v4:doctrine:begin 呢個常數註釋,無論 code 點都會綠,即係 test-shape 捉緊嗰個形狀;改成搵 # 開頭嗰行先紅得起。1,675 條 test 全綠。\\n\\n───\\n\\n2026-08-28,第六十二次:15276 → 15330(+54),全部喺 kernel/redgreen.py。買嘅係「條 test 有冇真係執行過呢個 function」由一種語言變三種。\n\n呢個機制本來掛喺一個塞入 `PYTHONPATH` 嘅 `sitecustomize.py`,所以佢為 Python 答,而對住 Go、TypeScript、Rust 一律回 `None` —— 「乜都冇睇到」。`None` 對一個 instrument 唔到嘅指令嚟講係誠實嘅,但佢當時掩住咗一個唔同嘅事實:Go 同 Node 兩邊都有官方機制,而冇人接。\n\n實測先,寫 code 後。同一個 A/B —— 一條斷言源碼文字嘅 test、一條真係 call 嗰個 function 嘅 test,兩條都過、兩個 exit 0 —— 三種語言各自分得開:Python `executed False/True`;Go `go tool cover -func` 報 `Target 0.0%` 對 `100.0%`;Node 由 V8 自己數,`執行次數 0` 對 `1`。\n\n兩個新入口都係環境變數,冇改過 repo 自己嗰句 test command:`GOFLAGS=-coverprofile=…` 同 `NODE_V8_COVERAGE=…`。`GOFLAGS` 係 append 唔係 replace,而且先檢查 repo 自己有冇設過 `-coverprofile` —— 有就唔搶,然後誠實答 `None`,因為搶咗會靜靜整壞人哋自己個 coverage 流程。\n\nRust 冇做,而理由唔係「未輪到」:`-C instrument-coverage` 要 `llvm-tools` component,即係「toolchain 喺度」唔等於「呢樣嘢用得到」,而一個喺部分安裝先行得到嘅機制正正係呢個框架拒絕嘅形狀。\n\n呢 54 行入面有一個真缺陷嘅修:`go tool cover -func` 本來用 profile 自己嗰個臨時目錄做 cwd,而佢要喺有 `go.mod` 嗰度先解析到 import path —— 所以 profile 寫咗、`go test` 報 `coverage: 100.0%`、而呢度照樣答「乜都冇睇到」。由一條 test 捉住,發現過程寫咗入 `_go_executed` 個 docstring。\n\n八條新 test,三種語言各兩條,加一條釘住「三種都 instrument 唔到就要答 None,唔可以變成告人」,加一條釘住「repo 自己嗰個 profile 要保住」。\n\n───\n\n2026-08-28,第六十一次:15260 → 15276(+16)。一次搬遷,唔係一次新增:`checkers` 跌 532、`kernel` 升 548。\n\nclaim 9971014d 講 `checkers/spec_coverage.py` 將 1052 行判斷擺喺 argv 同 exit code 後面,冇 detector 冇 test 攞得到,除非開一個 process 讀 exit code。搬走之後個 entry file 62 行,淨係定義 `main`,而六個 test module 由「開 process」變成 `import`。行為零改變,而證明唔係讀 code:23 個 fixture case 全過,同埋對真 SPEC 拆前拆後判詞逐字相同。\n\n落腳點係 `kernel/spec_coverage.py` 而唔係 SPEC §12 step 1 講嘅 `kernel/analysis/`,原因由 `layer-boundary` 即場報出嚟:`.v4/layers.json` 一個 import 都唔准 `analysis` 行,而呢個判斷嘅輸入就係 kernel —— `config.facts_path_for`(佢自己個 docstring 記低九個地方曾經各寫各嘅 glob,所以唔可以喺 analysis 重寫一次)同 subject repo 嘅 `kernel.doctrine`。個宣告個註釋記低歷史上唯一一條 `analysis → kernel` 邊係「修咗,唔係 baseline 咗」,所以宣告多一條就係用例外去答一條有機制執行嘅規則。§12 加咗一句講明呢種情況,因為要緊嘅唔係邊個目錄,係 import 得到。\n\n呢 16 行入面有一半唔係搬遷:`_reality` 新增一個 count。SPEC §4 講咗個數(「13 個薄 CLI」)而冇任何嘢核佢 —— 實測「21 個 checker 有 99 個薄 CLI」照樣 exit 0 —— 而「薄」冇一個重現得到嘅定義:淨係定義 `main` 出 7、同名 analysis module 出 9、inline 少過一百行出 14、有 import `kernel.analysis` 出 17。四個答案一個數字,正正係呢個 checker 存在嚟終結嗰個形狀。而家個文件講唯一一個唔使判斷嘅量法(個檔淨係定義 `main`,7/21),而 `counted_claims` 核佢:改做 99 就 exit 1。\n\n───\n\n2026-08-28,第六十次:15186 → 15260(+74)。四條 finding,一個共同形狀:一個檢查有能力答,而佢揀咗唔答。\n\n`kernel/accept.py::_test_detail`(+30,claim 5dd5e10a)—— `tests` 呢一行淨係留 suite 輸出最後一行,而一個 fail 咗嘅 run 最後一行係一個數目。`FAILED (failures=2, skipped=1)` 講幾多,永遠唔講邊兩條,而 `cmd_accept` 個 `TemporaryDirectory` 喺人讀到嗰行之前已經冇咗 —— 所以攞返個名要重建 archive 再跑一次成套 suite。實測 2026-08-27 兩個 session 同一日撞到,一個攞唔返。而家整份輸出寫落一個唔跟住棵樹死嘅檔並報路徑。**冇讀輸出格式**:`test_command` 由 repo 自報,一條知道邊行有名嘅規則就係將呢個 repo 寫入框架。\n\n`kernel/analysis/dangling_ref.py::go_scan` 同 `checkers/dangling_ref.py`(+28,claim c01c563e)—— `gosource.shape` 對「helper build 唔到」「build timeout」「冇裝 go」全部回同一個 None,而 `go_scan` 對三樣都 `continue`。所以乜都讀唔到嗰陣佢回 `[]`,checker 印 `every same-repo reference resolves` 然後 exit 0 —— 一句關於佢從來冇開過嘅檔嘅說話。`_go_package_names` 兩個 function 之上已經寫住 `no verdict, never \"no names\"`,呢次係同一條規則行到落有能力處理佢嘅 caller。而家回 `(findings, unread)`,checker 對 unread 出 exit 4。觸發佢嘅係一條 test unlink 晒 tempdir 入面每個 `v4-go-*`(已收窄),但令佢靜音嘅係呢度。\n\n`kernel/facts.py::restate`(+62,claim 981af8e0)—— `seen_at` 係 `file:line`,而個行號係呢個 module 自己推導得到嘅值:`scan_source` 講佢而家 match 邊行,`check_seen_at` 用嚟分「移咗」同「冇咗」。得「移咗」會被修,而修佢即係一個人手抄一個工具啱啱印出嚟嘅數字 —— 實測一行喺一日之內移咗兩次,兩次都係手改,兩次所有閘都綠。而家工具寫返佢自己算出嚟嘅嘢,而佢唔可以令一個缺陷消失:pattern 一個都 match 唔到就係 `gone`(`--gone-only` 失敗嗰樣),拒絕;match 多過一個就係一個判斷,拒絕並列出候選。\n\n`kernel/doctor.py`(+52)—— `facts` 呢一行淨係 `validate`,而 validate 讀嘅係表嘅形狀,唔係啲 row 仲指唔指得住嘢。所以一張每行都指住虛空嘅表,喺一個職責係「分開真接線同得個樣」嘅指令度印 `ok`。同一份報告隔兩行已經對受保護路徑同 `file::symbol` 問緊呢條問題;唯獨 detector 攞詞彙嗰張表冇問。實測喺 reference adopter:一日兩次 refactor 令 row 指住虛空,兩次全綠。會捉到第一次嗰個機制接咗喺呢個框架自己嘅 CI,而 `v4 install` 唔抄 workflow —— 所以義務去咗 adopter 而機制冇。\n\n呢 74 行冇買到嘅嘢:`checkers/spec_coverage.py` 冇分拆(claim 9971014d)。SPEC §12 step 1 確實規定判斷住喺 `kernel/analysis/<name>.py`,但實測 **12/21 個 checker 都冇跟**。修一個就係只修被報告嗰處,修十二個係一次冇批准嘅重新設計 —— 條規則要執行定要改,交返請求者。\n\n───\n\n2026-08-28,第五十九次:15162 → 15186(+24),全部喺 kernel。兩條「已經知道但唔肯用」。\n\n`ledger.reconcile_signatures` 用 `git log --all` 證明咗一個簽名紀錄 commit 咗喺第二條 branch,然後照樣當佢唔見咗,而 `fatal()` 保留佢 —— 所以並行嘅兩刀各自簽咗名就互鎖。實測 2026-08-27,呢個 repo 自己嗰次 wave 十三刀入面兩刀噉樣卡住,出路係 merge 其中一條入另一條。而 `fatal` 自己寫住係「代表有人改過紀錄嘅問題」,一個已經 commit 咗嘅檔唔符合佢自己個標準。呢度大部分行數係去開嗰個檔:`git show <rev>:<path>`,跑一模一樣三個驗證(parse、逐 field 對 row、入咗版控 —— 第三個由 commit 本身答,所以 `ls-files` 對呢條路唔再問,佢問嘅係本 branch 個 index)。`--diff-filter=d` 令個 rev 係「個檔仲存在」嗰個 commit,唔係「最後一次掂過佢」嗰個,否則一條 branch 上面嘅刪除會令另一條 branch 上面仲喺度嘅紀錄讀唔到。\n\n`state.claim_state` 六條比較全部 return `STALE` 一個字,而 `risk.route` 對六條講同一句「`v4 check` 再問一次就得」。對 detector-sha 嗰條係錯嘅:row 帶住提出佢嗰個版本嘅 sha,直到一次 re-derive append `claim_reraised`,所以 checker 答 PASS 而 state 答 STALE,永遠。實測喺呢個 ledger:claim `c8f3f1d4` 由 12:34 到 17:31 PASS 咗八次,八次之後都係 STALE,一次 `v4 derive` 就好返 —— 而八行 STALE 冇一行提過 derive。抽咗 `state.stale_reason` 出嚟,`claim_state` 同 `route` 讀同一個實作(兩份六條比較就係印出嚟嗰個原因同判詞開始分家嘅方法),`route` 對嗰條回 `REDERIVE` 唔係 `RERUN`。順帶包埋 `attempt.worktree` —— 佢一路存住路徑,而「你讀緊嘅樹唔係答佢嗰棵」之前同「個檔改咗」印同一個字,實測 `t-outran` 喺 main 讀係 13 STALE、喺佢自己嗰棵樹讀係 13 ANSWERED。\n\n───\n\n2026-08-27,合併輪:15119 → 15162(+43)。十五條「講嘅同做嘅唔同」:七條改 code(ts_imports 個 identity substitution、go_findings 個 substring 比對、_ts_scopes 攞錯 brace、ts_findings file-wide 上限、redgreen 對 None 嘅告白、scope.usage 數埋被拒絕嘅 widen、cmd_cover 掉走 faults 同 unspoken),六條改字,一條拒絕擴闊規則改為寫低一個 known_miss。\n\n呢個數唔係邊一刀嘅預測。今次 wave 十一刀全部對住 15018 度自己嗰個 delta,而每刀寫低嘅 ceiling 描述緊一棵之後就唔再存在嘅樹 —— 十刀已經落地。呢度個數字由 checker 喺合併後嗰棵樹度量出嚟,唔係由任何一刀嘅帳加埋。\n\n───\n\n2026-08-27,第五十七次:15018 → 15065(+47),全部喺 kernel。一次 lens sweep 出嘅 15 條 finding,一個事實:呢棵樹講自己做咗乜嗰啲字,同佢實際做嘅唔同。十三條係獨立事實,兩對係同一事實由兩個 lens 各睇一次。\n\n八條係改句字,零 statement:`fail_closed.py` 同 `secret_patterns.py` 個「No I/O here」(SPEC §12 早就數過 22 個 module 有 10 個讀檔、3 個行 git)、`engagement.py::judge_text` 一個 docstring 三個數(四/七/body 入面五,而 `judge` 個 inline comment 都寫住四)、`review.py::lenses` 用一個冇人 parse 嘅 `V4-CLAIM:` 行做自己存在嘅理由、`secret_patterns.py` `TEMPLATE_CHARS` 上面嗰個 literal URI(實測:`checkers/secret_scan.py` 單獨對住嗰個檔 exit 1,報 line 304)、`test_shape.py::source_assertions` 個 reflection 缺口(改成 `known_miss/` fixture,SPEC §12 要求嘅形狀)、同埋 `docs/SPEC.md` 講 `write_block.py` 唔睇 protected(連 `docs/RATIONALE.md` 第二處)。\n\n呢 47 個 statement 買嘅係七條真嘅判斷缺陷:\n\n`symbols.py::ts_imports`(+3)—— string pass 係 `_TS_STRING.sub(lambda m: m.group(0), ...)`,一個 identity substitution。改唔到做真 strip,因為一個真 import 個 specifier 本身就係 string;所以係搵出 string span,由 span 入面開始嗰個 `import` 唔算。實測:一個 template literal 入面嘅 import 之前當真 import 交去 `dangling_ref.ts_scan` 同 `layers.scan`。\n\n`fail_closed.py::_go_reports`(+6)—— `any(r in c for r in GO_REPORTS)` 對 callee 做 substring:`fmt.Sprintf` 含 `print`,`catalog.Get` 含 `log`。改成逐個 dotted segment 由開頭比對(Go 個 convention 係加後綴同用途做 receiver 名,所以 `logger.Info`、`metrics.Inc` 照樣算)。\n\n`external_write.py::_ts_scopes`(+6)—— `mask.find('{', m.end()-1)` 攞到參數表個 destructuring brace,一個 destructured 參數出一個一行嘅 body,個 write 跌返落成個檔嘅 span —— 呢個 function 存在就係為咗阻止嗰樣。改成行完 parameter parens 先搵 body brace,加一個 gap 檢查(concise arrow 冇 braced body,唔可以借下一個)。\n\n`test_shape.py::ts_findings` + `_ts_bounded`(+9)—— fan-out 上限係 file-wide substring,而且對住仲有 string literal 嘅文字,`const label = \"batch upload\"` 一句令成個檔收聲。改成 `_ts_mask` blank 埋 string,再問「呢個 fan-out 睇得到嘅 scope 入面有冇一個 identifier 係 bound」。順帶:`_TS_READ` 個 `\\s*` 喺 mask 上面食咗個路徑,所以 arg 由 paren 開始,路徑由原文切返出嚟。\n\n`redgreen.py::verify`(+2)—— `_run_traced` 特登返 `None` 表示 tracer 由頭到尾冇 attach 過,佢個註解寫明當 `False` 讀就係告個 worker 一條佢冇犯嘅罪;`verify` 寫 `if not executed:`,對 `None` 照告。兩者一樣唔算 ok(`symbol_executed` 留 `None`),但一個係未知,唔係指控。\n\n`scope.py::usage`(0)+ `cli.py::_refused_widens`(+4)—— `widens=len(rows)` 連被拒嘅 widen 都數,而佢上面嗰句註解講明「把每行加埋」就係要修嗰個缺陷。`refused` 同 `reached` 兩個 key 之前冇 reader,而家三條 widen 行都印。\n\n`cli.py::cmd_cover` + `_print_unspoken`(+17)—— `measure()` 返六樣,佢印四樣;掉走嘅兩樣正正係講「呢盤數係錯」而唔係「未夠」嗰兩樣:一個 `fault()` 拒收咗嘅 entry 照樣顯示成 delivered,而 `unspoken`(冇人引過嘅 clause,字元比例睇唔到嗰個)全 repo 冇 reader。\n\nRed-green:四個新 test 檔對住 `0785a00` 各自紅過,每條 claim 行入過自己嗰個 symbol。另外三個 fixture case 釘住 checker 嗰邊 —— `external_write/bypass/ts_the_parameters_are_destructured.ts`、`test_shape/bypass/ts_a_bound_named_only_in_a_string`、`fail_closed/bypass/go_the_report_is_only_a_formatted_string.go`,三個喺呢個改動之前都 exit 0。\n\n───\n\n2026-08-27,合併輪:15088 → 15119(+31)。七份私人副本出、五個 owner 入 —— owner 貴啲,因為副本乜都唔做(一個把每個 exit code 塌成空字串嘅 nested git()、一個 dict literal、一個 fnmatch),而 owner 要帶住有型別嘅失敗同必填參數。\n\n呢個數唔係邊一刀嘅預測。今次 wave 十一刀全部對住 15018 度自己嗰個 delta,而每刀寫低嘅 ceiling 描述緊一棵之後就唔再存在嘅樹 —— 十刀已經落地。呢度個數字由 checker 喺合併後嗰棵樹度量出嚟,唔係由任何一刀嘅帳加埋。\n\n───\n\n2026-08-27,第五十七次:15018 → 15026(+8)。一次去重跑出嚟嘅淨增長,因為換走嘅七份副本細,而接手嗰五個 owner 大。\n\n拆開睇,冇一個檔係加嘢:\n  -26 hooks/stop_gate.py      `_v4_home`,加四份 `git rev-parse --git-common-dir` + `v4/ledger.db`\n  -11 checkers/test_weakened.py  `_count`\n  -10 hooks/write_block.py    `_ledger`(第五份)\n   -9 detectors/test_weakened.py `_count`(同上面嗰個逐 byte 一樣)\n   -4 kernel/derive.py        `_in_scope` 同 `_filters_matching_nothing` 入面第二個 fnmatch\n   -2 kernel/cli.py           三句 accepted-risk SQL\n  +24 hooks/_framework.py     `home()` 同 `db_path()`\n  +18 kernel/runner.py        `Subject` 同 `subject_params`\n  +10 kernel/analysis/test_weakened.py `count_for`\n   +9 kernel/hashing.py       `GitUnreadable`、`_git`、`_is_repo`\n   +7 kernel/risk.py          `signed_by_kind`、`signed_line`\n   +2 kernel/analysis/subject_files.py `is_test` 識得 `conftest.py`\n\n呢 8 個 statement 買咗嘅係「一份實作」同「一份會答錯嘅實作」之間嗰段距離。副本平係因為佢哋乜都唔做:一個 nested `git()` 把每個 returncode 塌成 `\"\"`、一句 dict literal、一個 fnmatch。接手嗰個唔可以咁:`hashing._git` 要一個 typed failure(`GitUnreadable`)同一個 `_is_repo`,因為「git 喺一個 repo 入面拒絕」同「呢度唔係 repo」本來係同一個值 `{}`,而呢個 module 計嘅係 staleness digest;`runner.subject_params` 要 `derive_exclude` 做冇 default 嘅必填參數,因為三份 payload 唔同就係喺呢個 key 上面(`register` 個 probe 冇送,`lifecycle` 有);`_framework.db_path` 要問返 `kernel.ledger.ledger_path`,而 hook 又要喺冇 kernel 嗰陣仍然行得,所以係一個 owner 加一條 fallback,唔係一句 `subprocess.run`。\n\nRed-green:`tests/test_one_rule_with_one_implementation.py` 23 條,對住 `0785a00` 有 21 條紅。餘下兩條(`test_and_a_file_outside_the_scope_is_still_refused`、`test_and_a_real_config_module_still_is_one`)兩邊都綠,係故意嘅:呢一刀每個修都係用一個闊啲嘅答案換一個窄嘅,呢兩條釘住唔應該跟住郁嗰個方向。\n\n───\n\n2026-08-27,合併輪:15056 → 15088(+32)。spec_coverage 個 corpus 由 docs/*.md 擴到包埋 SPEC 12.5 定為契約嘅七個 .claude/ 檔,加 command_rows 同 _facts_rows。三個範圍第一次被檢查。\n\n呢個數唔係邊一刀嘅預測。今次 wave 十一刀全部對住 15018 度自己嗰個 delta,而每刀寫低嘅 ceiling 描述緊一棵之後就唔再存在嘅樹 —— 十刀已經落地。呢度個數字由 checker 喺合併後嗰棵樹度量出嚟,唔係由任何一刀嘅帳加埋。\n\n───\n\n2026-08-27,第五十七次:15018 → 15050(+32),全部喺 `checkers/spec_coverage.py`(541 → 573 statement,實測),`kernel/`、`detectors/`、`hooks/` 一個都冇郁。\n\n`spec-coverage` 係全個 repo 唯一一個把一句文件寫住嘅 `v4 …` 對返 CLI 嘅嘢,所以佢唔開嗰啲檔,就係冇任何嘢檢查過。佢有三個範圍由頭到尾冇開過,而三個互相扣住:\n\n- `user_facing_docs` 個集合係 `docs/*.md` + `CLAUDE.md` + `.github/monitor/*.md`,剔走咗 SPEC §12.5 定為契約嗰四個 `.claude/agents/` 角色同三個 `.claude/commands/`。實測 2026-08-27:`bash -lc \"v4 --repo . review lens\"` —— `reviewer.md` 第 10 行,一個 reviewer 開工第一句 —— exit 127,command not found。七個檔全部係同一個形狀。\n- 反方向嗰個指令檢查淨係問「`v4 <名>` 呢個 string 有冇喺 SPEC 出現過」,所以 §13 個指令表 27 行對住 32 個 subcommand,漏咗 `trend`、`foresee`、`remerge`、`sweep`、`cover`,而 checker 綠,因為五個每個都喺第二個章節被提過一次。\n- `_reality` 為 checker、kind、lens、detector、hook、agent、subcommand、pin 都結算得到數目,facts 表冇。所以 `docs/FACTS.md` 講「7 rows」由第八行 `_matches_auth` 2026-08-20 落地嗰日起冇任何嘢結算得到 —— 而 `FACTS.md` 自己就係嗰份話「一張講少咗嘅表令每個 detector 讀出一個乾淨 repo」嘅文件。\n\n呢 32 個 statement 買咗:`user_facing_docs` 用 glob 把 `.claude/agents/**` 同 `.claude/commands/**` 收入集合(所以第八個角色檔唔使改 code 就有人讀);`command_rows` 同 `check` 入面嗰條分支(一份 SPEC 有指令表,張表就係嗰張清單,`cli_commands` 每一個都要有自己一行;冇表嘅 SPEC —— adopter 嗰份得一頁半 —— 照舊只被問「有冇提過」,同 `engaged_kinds_explained` 學過嗰件事一樣);`_facts_rows` 由 `kernel.config.facts_path_for` 讀表,逐個 list-valued key 出一個數(加一張表唔使改呢個 function);`_count_re` 把 quantifier 變成可選加 `_names_its_population` 守住佢(一句冇中文量詞嘅數,要講出佢數緊邊張表先算數 —— 實測:淨係 drop quantifier,`docs/` 由 0 條變 50 條,`v4 check` 被讀成四個 check 係最短嗰個);同埋 `flags_resolve` 個 `where`,佢之前印 `docs/<名>`,而一份 `.claude/agents/reviewer.md` 嘅 finding 會印成 `docs/reviewer.md` —— 一條冇人開得到嘅路徑,喺一句唯一嘅工作就係叫人去睇嗰行度。\n\nFixture:23 個 case(10 red / 8 green / 5 bypass),新加 `a_command_the_table_leaves_out`、`a_role_document_nobody_can_paste`、`a_facts_row_count_that_moved`,bypass 加 `two_commands_in_one_table_cell`(一格入面兩個指令,睇落齊,而個 cell 唔係任何一個嘅行)同 `a_role_document_that_indents_instead_of_fencing`(四格縮排唔開 fence —— `.github/monitor/PROMPT.md` 用過嗰個形狀)。\n\nRed-green:`tests/test_the_documents_this_checker_never_opened.py`,三條 claim 各自對住 `0785a00` 紅過(`user_facing_docs` 3 條 failure、`cli_commands` 1 failure + 2 error、`_reality` 2 條 failure),而且每條都真係行入過佢自己嗰個 symbol。文件嗰半同 code 一齊改:七個 `.claude/` 檔改成 `./bin/v4`(所以 `launcher_is_reachable` 由 7 條變 0 條)、SPEC §13 補返五行(27/32 → 32/32)、`FACTS.md` 由「7 rows」改成「8 auth_decision rows」。\n\n───\n\n2026-08-27,第五十七次:15018 → 15035(+17)。`--as-monitor` 出咗貨,而佢個例子、佢個報表、佢個訊息同佢四條 test 全部對同一件事講錯咗 —— 八條 finding,一個根因:**冇一個欄記住邊個簽**。\n\n`accepted_risk` 只有 `was_tty`。三條路去到一個簽名,其中兩條(`--no-tty-check` 同 `--as-monitor`)都係 `was_tty = 0`,所以 `trend.signatures` 同 `v4 ship` 個 `signed :` 行 —— 唯一答「邊個簽」嗰兩個地方 —— 將一個第二 session 嘅簽名同一個 worker 簽走判佢自己嗰條 claim 嘅簽名,數埋同一個數。`signed_by` 淨係住喺 `.v4/risks/<claim>.json` 入面,而報表一個都唔讀嗰個檔。\n\n呢 17 個 statement 買咗:`accepted_risk.signed_by` 同佢個 migration(舊 row 係 '',ledger 拒 UPDATE,補唔到,所以每個讀嗰邊都要有嗰個桶);`SIGNATURE_FIELDS` 嘅第四個欄位(邊個 column 空咗算「row 寫喺 column 之前」而唔算兩邊唔夾 —— 冇佢,181 條歷史簽名會永遠令 `v4 ship` 印 chain: BROKEN);`risk` 嘅 `PERSON/AGENT/MONITOR`、`UNRECORDED`、`TTY_ROUTES`(五段文同兩個報表 spell 緊佢哋,而第三條路加嗰日兩個報表仲係兩個桶);`trend.signatures` 個 `by_signer`;同 `cli._signatures_for` —— 三個地方問過同一條 `GROUP BY ar.kind`,而「邊個簽」邊個都冇問過。\n\n根因唔止一半。`risk.accept` 個 `as_monitor` 守衛讀 `claim.origin`,而 `origin` 講嘅係一條 claim **點樣**被造出嚟,唔係**邊個**造。所以一條由另一個 reviewer session 開嘅 review-finding 一樣被拒,而理由印住「so it is yours, so you cannot sign it」—— 對嗰個輸入嚟講係假嘅。冇欄補得返:同一個 repo 兩個 session 共用一個 git identity,`v4` 亦都唔知邊個 session 起佢,所以一個 `claim.raised_by` 會兩邊寫同一個字 —— 一個有 writer 冇 decision 用嘅欄,即係呢個 repo 自己個 `dead-wiring` 揾緊嗰隻。守衛改成問返 `origin` 答得到嗰條(係咪一個 program raise),拒收整個 hand-raised class,而拒收嘅句子講返佢知道乜。呢半係 0 statement:同一條 `if`,改咗個名同段文。\n\nRed-green:四個 mutant,逐個殺過。守衛搬去寫 record 之後 → `tests/test_kernel.Signing.test_and_not_the_finding_it_filed_itself` 紅(磁碟上留低 `own.json`);monitor 分支跳過 `accepted_risk` insert → `test_a_monitor_signs_what_a_detector_raised` 同 `test_the_three_routes_are_told_apart` 紅;剷走 `cmd_risk` 個 `as_monitor=args.as_monitor` → `TheMonitorFlagReachesTheKernel` 兩條紅;剷走 argparse 個 flag → 四條全部 error。另外 `Signing` 由讀 ambient `sys.stdin.isatty()` 改成 stub:改之前 `pty.spawn` 落去 12 條有 6 條紅,改之後 14 條全綠。\n\n───\n\n2026-08-27,第五十七次:15018 → 15037(+19),`kernel/sweep.py` +8、`kernel/cli.py` +11。**冇任何嘢降返落嚟,呢個 ceiling 又升咗一次 —— 呢句係記錄,唔係辯解。**\n\n上一次(第五十六次)嘅 +57 入面有一半係起 `record_lens_reviewed`,用嚟分開「一份 brief 印咗」同「有人審過」。嗰次只修咗 `v4 ship` 一邊。Sweep 嗰邊嘅 durable 紀錄一路淨係存 `ran`,而 `ran` 由 `lens_run` 嚟 —— `v4 review lens` 一印 brief 就寫,而印 brief 係免費嘅。Sweep 側零個 reader 掂過 `lens_reviewed`,所以 13 個 reviewer 攞晒 brief、一個都冇返嚟審,個紀錄照樣寫住 `ran: 13`。\n\n實測 2026-08-27:嗰次 sweep 開咗 11 個 reviewer,11 個全部有 `lens_run` 同 `lens_reviewed`,而 `v4 sweep --done` 印出嚟係 `12 of 13 printed their brief since the last sweep`。第 12 個係 `request-fidelity`,佢喺個 window 入面嘅 `lens_run` row 寫喺 `fw-rust`、`fw-reqfid`、`fw-tax` 度 —— 三個 worker 讀三份 diff,被當成一棵冇人望過嘅樹嘅覆蓋面。\n\n呢 19 個 statement 買咗:`sweep._lens_events`(一個 window,兩條 clause —— 上次 sweep 之後,加埋唔帶 task 嗰啲 row;ship 有 task 所以 key `task_id = ?`,sweep 冇 task,呢個係兩邊唯一唔同嘅位)、`sweep.reviewed_since`(`{lens: findings}`,last row wins)、`record` 入面多存一個 `reviewed`、`v4 sweep --done` 嗰三個狀態(報咗連數目 / 攞咗 brief 冇返嚟 / 一個 brief 都冇印過)、同 `--history` 分開印 briefed 同 reviewed(舊 row 冇 `reviewed` 呢個 key,印做 `0 reviewed` 就係喺度作一個嗰行從來冇量過嘅數,所以 `None` 同 `{}` 分開處理)。\n\n冇買一道閘:兩邊都唔會因為冇人審而拒絕,理由 SPEC §10.2 度過 —— 一個 lens 係判斷,閂喺「有冇人判斷過」上面買到嘅係一個剔。\n\nRed-green:`tests/test_the_cli_says_what_happened.py::ASweepPrintsWhoCameBackNotWhoTookABrief`(5 條,其中 4 條對住 `0785a00` 紅過,第 5 條係守住舊 row 唔可以被讀成 `0 reviewed`)同 `tests/test_kernel.py::TheAfterGateIsPeriodic` 新加嗰 3 條(全部對住 `0785a00` 紅過,其中一條紅嘅內容就係 `ran == ['request-fidelity']`)。每條都真係行入 `sweep.record` / `cmd_sweep`。\n\n───\n\n2026-08-27,第五十七次:15018 → 15056(+38),全部喺 kernel/analysis/shell_command.py。Shell guard 由「認得出寫入」反轉成「認得出讀取」。\n\n上一版問嘅係「呢句係咪一個寫入」,而答案由 `WRITERS` / `SUBCOMMAND_WRITERS` / `INPLACE` 三張表決定 —— 認唔出就當冇事。實測四條路行得過,而四條全部唔係表漏咗一個念頭,係漏咗一個拼法:`python3 -c \"open('.v4/config.json','w')\"` 同 `node -e`(一個 interpreter 由 argv 根本判唔到,佢收到嘅係一個程式)、`rm /path/to/repo/.v4/config.json`(同一個檔,由 root 拼)、`git -C . rm`(一個帶值嘅 global option 食咗個 subcommand)、`sed --in-place` / `perl -pi -e`(一個 flag 幾個拼法)。同一個寫入嘅正宗拼法全部拒。再補四行,第五個拼法照樣喺度 —— `sudo`、`xargs`、`env`、`unlink`、`ed` 當時全部行得過,而佢哋一個都唔係新念頭。\n\n所以問題改成「呢個 guard 講唔講得出佢淨係讀」,三個答案唔係兩個:講得出邊個檔被寫(拒,連檔名)、認得係唯讀(放)、其餘全部落 `None`(拒,理由係讀唔明)—— 同一句 unparseable syntax 到嘅同一個出口。\n\n呢 38 個 statement 買咗:`READERS`(而家係唯一一張必須完整嘅表,而佢唔完整嘅代價係一句寫住原因嘅拒絕,唔係靜音)、`SUBCOMMAND_READERS` / `SUBCOMMAND_DEFAULT_READS` / `VALUE_OPTS` + `_subcommand`(`git status .v4` 要繼續免費,所以 git 唔可以簡單當 unclassified)、`_spellings`(一個 argv 字唔係 repo-relative path,佢係一個路徑嘅拼法;normalise 完先交俾 `subject_files.matches` —— 唔係第七個 matcher)、`_in_place`(短 flag 當字母 cluster 讀)、同段 unreadable 分支。`WRITERS` 同 `INPLACE` 冇刪,但佢哋唔再係閘:佢哋而家買嗰句「邊個檔」嘅句子。\n\n38 入面有 1 個係反轉自己揾返嚟嘅,唔係 lens 報嘅:`v4` 自己嘅 argv 成日帶住 protected path —— hook 印嗰句解決方法本身就係 `v4 scope widen --add <嗰個 protected path>`,而令呢次改動落得到地嗰個 widen 就係其中一句。唔classify 佢,個 guard 會拒絕自己印出嚟嗰句。所以 `v4` 嘅未列 verb 當讀(佢寫 `.v4/` 但唔係由 argv 攞路徑,而且行嘅係 kernel —— 正正係呢個 guard 要守住嘅路),`export --out <path>` 係佢唯一由 argv 寫路徑嘅 verb,擺咗入 `SUBCOMMAND_WRITERS`。\n\n買到嘅覆蓋面(全部實測,44 條拒 + 28 條放,0 錯):四條 finding 全閂,另加 `sudo rm`、`xargs rm`、`env sed -i`、`bash -c \"rm ...\"`、`unlink`、`ed`、`busybox rm`、`cd .v4`、`git -C . <未知 verb>`、`v4 export --out .v4/...`、`$PWD/.v4/...`、`../wt-other/.v4/...`。\n\n收嘅費:`sed -n 1,20p checkers/scope.py` 由放變拒 —— 因為要當 sed 係 reader 就要再信一次 flag 拼法,而嗰樣嘢已經衰咗四次。改寫係 `cat checkers/scope.py | sed -n 1,20p`,protected path 坐喺讀嗰段。個 hook 嘅拒絕文本直接講埋呢句。\n\nRed-green:`tests/test_a_write_has_more_spellings_than_a_guard_has_rows.py`,對住 `0785a00` 20 條 test 入面 23 個 subtest 紅、2 個 error,而四條 control(`git status .v4`、`python3 -c \"print(1)\"`、唔存在嘅路徑、正宗拼法仍然係寫入)兩邊都綠。\n\n───\n\n2026-08-27,第五十七次:15018 → 15066(+48:hooks +21、kernel +26、checkers +1)。一個入口問「邊個」嗰陣問錯咗 —— 邊個 repo、邊個 task、邊個環境、邊個檔集。\n\nhooks 嗰 +21 係淨數:加咗 `hooks/_framework.py::repo_root`,同時 `hooks/stop_gate.py` 四份 `git rev-parse --git-common-dir` 收成一個 `_db()`(唔收嘅話呢次改動會加到第五份)。三個 hook 本來各自寫 `Path(os.environ.get(\"V4_REPO\", \".\")).resolve()`,而 `.claude/settings.template.json` 出廠 `V4_REPO` 就係 `\".\"`。實測:同一個受保護路徑嘅 Write payload,喺 repo root 拒得到,喺同一個 repo 嘅子目錄 `relative_to` 拋 ValueError、返 `{}`(= 放行)、而且一行 `hook_seen` 都冇寫 —— 一句 `cd` 熄咗個閘,喺 ledger 度同「從來冇裝過」一模一樣。而家一個目錄只係 `git rev-parse --show-toplevel` 嘅起點,唔係佢嘅答案,而 template 唔再出廠嗰個值。\n\nkernel 嗰 +26:`stop_gate._open_task` 由「攞最新嗰個」改成同 `write_block.open_task` 一樣嘅 `AMBIGUOUS`(先用 session 收窄,收唔窄就講出邊幾個 task 開住),連埋 `ledger.sessions_on` 唔再數 `scattered` 嗰啲 mark —— `bash_guard` 同 `write_block` 喺分唔清邊個 task 嗰陣會逐個開住嘅 task 都寫一行,而嗰啲行本來令 `_worked_here` 認低一個佢從來冇掂過嘅 task。`ledger.attempt_reader` 收咗 `lifecycle` 同 `state` 兩份同樣嘅 reader(前者一 call 就 `NameError`,因為 1,082 條 claim、492 個 subject ref 全部係 `kind: \"file\"`,由頭到尾冇人行過)。`risk.commit_anchor` 喺簽名嗰刻真去問 `git log -1 --format=%G?` 同 `commit.gpgsign`,所以「呢個檔存在於一個 commit 入面係 durable 嗰半」唔再係一句冇人核過嘅話。`runner.child_env` 由 `dict(os.environ)` 改成一張 allow-list。`cli.cmd_init` 寫返 `\"doctrine\": true`。\n\nRed-green:`tests/test_which_repo_which_task_which_environment.py`,九條 claim 每條對住 `0785a00` 各自紅過,而且每條都行入過佢自己嗰個 symbol;另外三條係 text closure(`.claude/settings.template.json` 同 `tools/surface_probe.sh` 冇 frame 入得)。\n\n───\n\n2026-08-27,第五十七次:15018 → 15055(+37)。hooks 507 → 523,kernel 11040 → 11061,checkers 同 detectors 淨值零。一個事實七個位:一個吞咗嘅 exception 令「呢個檢查跑唔到」同「呢個檢查跑咗冇嘢」印同一句。\n\n呢七處嘅 recovery 本身冇錯 —— hook 照放行、redaction floor 照過濾、detector 照掃其餘嘅檔。錯嘅係佢哋全部用一個乾淨答案嘅返回值嚟講一件冇發生過嘅事:`_print_uncovered` 個 `except Exception: return` 由 ship report 度剷走成行「未問過嘅風險類別」;`write_block.open_task` 個 `except sqlite3.Error: return None` 同「ledger 讀過,冇 task 開住」係同一個值;`redaction.redact` 個 `except Exception: pass` 喺一個 append-only、會 commit 嘅 export 之前退返去 `_REDACTIONS` 嗰三個 shape;`_framework._module` 掉走 import exception,而佢個理由(`why` 會講)行 `on_path`,`on_path` 喺呢個 case 成功時返 `None`;`registry_consistency._absence_still_holds` 兩條路都返 `[]`,而 `[]` 就係 PASS;`fail_closed` detector 個 Go 分支用一個 vestigial `pass` 跳過,隔籬三個 skip 全部 warn;`doctrine.drift` 用 `have_text.index(END)` 重新做一次 `split` 已經做咗嘅嘢,於是一個 END marker 唔見咗嘅 CLAUDE.md 掟 `ValueError` 出嚟(實測:`ValueError substring not found`)。\n\n呢 37 個 statement 買咗:`coverage.RubricUnreadable` —— 「冇 rubric」同「rubric 讀唔到」嘅分家住喺 `coverage.rubric`(即係嗰條問題嘅 truth owner)而唔係兩個 caller 各做一次 `is_file()`,因為兩個 caller 都喺 `kernel/cli.py`(`_print_uncovered` 掉走成行,`cmd_coverage` 對住一個明明喺度嘅檔印「no .v4/risk_rubric.json here」),而兩處各自 derive 同一條界就係 `doctrine.drift` 呢一刀入面拆走嗰個 shape;`cli._print_unasked_unreadable` 同 `cmd_coverage` 嗰條 `RubricUnreadable` 分支;`write_block.UNREADABLE` sentinel、`open_task` 兩條 stderr 路(kernel 攞唔到、ledger raise)、同 `main` 入面唔准佢當 `None` 嘅分支;`_framework._IMPORT_FAILED` 同 `why` 讀返佢(key 用 `(root, module)`,唔係淨 root,否則 `config` import 得就抹走 `ledger` import 唔到嘅紀錄);`redact` 拆開兩種失敗 —— module import 唔到(得返 `_REDACTIONS`,講出嚟)同 repo table 讀唔到(跌落 `PATTERNS` 十二個 family,唔係五個 prefix,再講出嚟);`registry_consistency` 兩句 problem string;`fail_closed` detector 一句 `warn`;`doctrine.drift` 改行 `split` 返嘅 `before`/`after` 加一條指名「BEGIN 冇 END」嘅分支。\n\n淨值零嘅兩個 area 係真嘅:checker 嗰兩處係 `return []` 換 `return [句]`,detector 嗰處係 `pass` 換 `warn(...)`。加價全部喺 hooks 同 kernel。\n\n冇加嘅嘢:冇一處改成 raise。`redact` 個 docstring 講「a floor that raises an exception is worse than a narrow one」,而喺 append-only ledger 呢條路上,掉失成條 attempt row 比掉失一個 credential family 更差,所以跌落更闊嗰張表 + 講出嚟,而唔係炸。\n\nRed-green:`tests/test_a_check_that_did_not_run_says_so.py`,21 條,對住 `0785a00` 紅過,七條 claim 每條至少一條紅,而且七條全部行過 `kernel/redgreen.verify`:red_failed_at_parent、green_passed_at_head、symbol_executed 三樣皆真(calls 1 到 4 不等)。其餘幾條係另一半 —— 冇 rubric 要照樣靜、ledger 讀得到冇 task 要照樣返 `None`、pattern table 讀得到要照樣唔出聲 —— 佢哋喺兩邊都綠,存在係為咗釘住「唔好靜」冇變成「乜都嘈」。\n\n───\n\n2026-08-27,第五十七次:15018 → 15026(+8),kernel +7、hooks +1。買咗兩樣嘢:一個 event kind 詞彙表連佢個拒收,同一個有名嘅拒絕。\n\n`event.kind` 全個 repo 淨係喺 `kernel/ledger.py` 個 SCHEMA 註解度列舉過一次,佢寫六個(`scope_widen|engagement|detector_run|blocked|remerge|register`),而 AST 數過 kernel/ 同 hooks/ 每個 `insert(conn, \"event\")` 係三十二個 literal kind(加一個 dynamic:`register._seed_ledger` 讀 fixture 宣告嘅),現存 ledger 有二十七個。即係一個查 ledger 嘅人見到 `hook_seen`、`checker_out`、`lens_reviewed` 呢啲名,冇任何地方查得返佢係乜。`runner.record` 為 `cost_observation.source` 修過同一個遺漏,佢留低嘅規則係「個註解就係講個 set 嘅地方」—— 而呢個註解漂咗二十六個名,證明咗一個註解揸唔住。\n\n呢 8 個 statement 入面 5 個喺 `kernel/ledger.py`:`EVENT_KINDS`(三十二個名,每個一句寫俾讀 ledger 嗰個人)、`UndeclaredEventKind` 同佢嘅 docstring、`insert` 入面嗰個 `if kind not in EVENT_KINDS: raise`。個拒收擺喺 write gate 開之前:呢張表 append-only,一個查唔到嘅名寫咗落去就永遠喺度,所以加一個 kind 而家必然係同一個 diff 入面加一行解釋。實測即刻捉到一個:`tests/test_kernel.py` 有個 fixture 寫緊 `derive`,一個 kernel 從來唔會 emit 嘅名。\n\n餘下 3 個:`kernel/cli.py` 嘅 `root = _repo(args)`(`export_jsonl` 而家收一個必填 repo root,因為佢最後嗰道 redaction filter 要讀 `<root>/.v4/secret_patterns.json`;實測一個宣告咗 `whsec_` 家族嘅 repo,個 token 原文入咗 `.v4/ledger_export.jsonl`)、`kernel/doctor.py` 個 `run()` docstring(嗰二十六個截一半嘅 check 名而家講得出自己問乜),同 `hooks/stop_gate.py` 淨 +1:`refuse_the_stop` 四個 statement,減返三個 call site 各自省一個(本來 `print(json.dumps(...))` 加 `return 0`,而家一句 `return refuse_the_stop(...)`)。呢個 hook 出三個 `decision: block` 而 `.v4/facts.auto-dev-v4.json` 冇一行指得到佢,一個有名嘅拒絕先至令張表指得住嘢。\n\nRed-green:`tests/test_what_leaves_the_ledger_and_what_a_kind_means.py` 同 `tests/test_a_declaration_with_no_reader.py`,五條 claim 各自對住 `0785a00` 紅過,每條都行入過佢自己嗰個 file/symbol(`_redact`、`kernel/ledger.py`、`facts_grammar.py`、`stop_gate.main`)。\n\n───\n\n2026-08-27,第五十七次:15018 → 15055(+37)。hooks 507 → 523,kernel 11040 → 11061,checkers 同 detectors 淨值零。一個事實七個位:一個吞咗嘅 exception 令「呢個檢查跑唔到」同「呢個檢查跑咗冇嘢」印同一句。\n\n呢七處嘅 recovery 本身冇錯 —— hook 照放行、redaction floor 照過濾、detector 照掃其餘嘅檔。錯嘅係佢哋全部用一個乾淨答案嘅返回值嚟講一件冇發生過嘅事:`_print_uncovered` 個 `except Exception: return` 由 ship report 度剷走成行「未問過嘅風險類別」;`write_block.open_task` 個 `except sqlite3.Error: return None` 同「ledger 讀過,冇 task 開住」係同一個值;`redaction.redact` 個 `except Exception: pass` 喺一個 append-only、會 commit 嘅 export 之前退返去 `_REDACTIONS` 嗰三個 shape;`_framework._module` 掉走 import exception,而佢個理由(`why` 會講)行 `on_path`,`on_path` 喺呢個 case 成功時返 `None`;`registry_consistency._absence_still_holds` 兩條路都返 `[]`,而 `[]` 就係 PASS;`fail_closed` detector 個 Go 分支用一個 vestigial `pass` 跳過,隔籬三個 skip 全部 warn;`doctrine.drift` 用 `have_text.index(END)` 重新做一次 `split` 已經做咗嘅嘢,於是一個 END marker 唔見咗嘅 CLAUDE.md 掟 `ValueError` 出嚟(實測:`ValueError substring not found`)。\n\n呢 37 個 statement 買咗:`coverage.RubricUnreadable` —— 「冇 rubric」同「rubric 讀唔到」嘅分家住喺 `coverage.rubric`(即係嗰條問題嘅 truth owner)而唔係兩個 caller 各做一次 `is_file()`,因為兩個 caller 都喺 `kernel/cli.py`(`_print_uncovered` 掉走成行,`cmd_coverage` 對住一個明明喺度嘅檔印「no .v4/risk_rubric.json here」),而兩處各自 derive 同一條界就係 `doctrine.drift` 呢一刀入面拆走嗰個 shape;`cli._print_unasked_unreadable` 同 `cmd_coverage` 嗰條 `RubricUnreadable` 分支;`write_block.UNREADABLE` sentinel、`open_task` 兩條 stderr 路(kernel 攞唔到、ledger raise)、同 `main` 入面唔准佢當 `None` 嘅分支;`_framework._IMPORT_FAILED` 同 `why` 讀返佢(key 用 `(root, module)`,唔係淨 root,否則 `config` import 得就抹走 `ledger` import 唔到嘅紀錄);`redact` 拆開兩種失敗 —— module import 唔到(得返 `_REDACTIONS`,講出嚟)同 repo table 讀唔到(跌落 `PATTERNS` 十二個 family,唔係五個 prefix,再講出嚟);`registry_consistency` 兩句 problem string;`fail_closed` detector 一句 `warn`;`doctrine.drift` 改行 `split` 返嘅 `before`/`after` 加一條指名「BEGIN 冇 END」嘅分支。\n\n淨值零嘅兩個 area 係真嘅:checker 嗰兩處係 `return []` 換 `return [句]`,detector 嗰處係 `pass` 換 `warn(...)`。加價全部喺 hooks 同 kernel。\n\n冇加嘅嘢:冇一處改成 raise。`redact` 個 docstring 講「a floor that raises an exception is worse than a narrow one」,而喺 append-only ledger 呢條路上,掉失成條 attempt row 比掉失一個 credential family 更差,所以跌落更闊嗰張表 + 講出嚟,而唔係炸。\n\nRed-green:`tests/test_a_check_that_did_not_run_says_so.py`,21 條,對住 `0785a00` 紅過,七條 claim 每條至少一條紅,而且七條全部行過 `kernel/redgreen.verify`:red_failed_at_parent、green_passed_at_head、symbol_executed 三樣皆真(calls 1 到 4 不等)。其餘幾條係另一半 —— 冇 rubric 要照樣靜、ledger 讀得到冇 task 要照樣返 `None`、pattern table 讀得到要照樣唔出聲 —— 佢哋喺兩邊都綠,存在係為咗釘住「唔好靜」冇變成「乜都嘈」。",
  "measured_at": "2026-08-27"
}
