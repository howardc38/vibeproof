# RED. Deploy credentials pasted into CI config. Sits deliberately next to a
# postgres:postgres service container, which is green -- the checker has to
# split the two, and the V3 scanner could not.
services:
  postgres:
    env:
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/postgres"
env:
  AWS_ACCESS_KEY_ID: AKIALU3NDADVWPP7FXBG
