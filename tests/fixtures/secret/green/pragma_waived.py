# GREEN. Carried over from V3's line-scoped waiver. An operator who has decided
# a value is safe says so explicitly, once, on the line above it -- and the
# waiver expires after that one line.
# Rule: pragma-waived (the value is blanked before matching, so nothing matches).

# pragma: allow-secret
DOCUMENTED_SAMPLE = "AKIAQWERTYUIOPASDFGH"
