{
  "test_command": "true",
  "policy": "allow_accepted_risk",
  "truth_command": "python3 -c \"import sys; exec(sys.stdin.read())\"",
  "runtime_proof": [
    {
      "name": "the answer was already there",
      "trigger": "true",
      "truth": "print('ok')",
      "expect": "contains:ok"
    }
  ]
}
