{
  "test_command": "true",
  "policy": "allow_accepted_risk",
  "truth_command": "python3 -c \"import sys; exec(sys.stdin.read())\"",
  "runtime_proof": [
    {
      "name": "never false",
      "trigger": "python3 -c \"open('s_{run_id}','w').write('0')\"",
      "truth": "print(open('s_{run_id}').read().strip() if __import__('os').path.exists('s_{run_id}') else '')",
      "expect": "gt:-1"
    }
  ]
}
