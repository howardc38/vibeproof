## The gate
<!-- pinned: -->

`spec-coverage` asks that a section describing a mechanism carry at least one
pin. An empty marker satisfies that question and resolves to no file and no
symbol, so the section reads as wired up and is connected to nothing.
