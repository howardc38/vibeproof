The marker is written `<!-- pinned: src/gone.py -->`, and a sentence showing
the syntax is not pinning anything -- reading it as a pin reports the document
for describing itself.

<!-- pinned: src/core.py::worktree_digest -->
