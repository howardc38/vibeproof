{
  "parent_content": {
    "src/parse.rs": "#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn parses_a_selector() {\n        assert_eq!(parse(\"a\").is_ok(), true);\n    }\n}\n"
  },
  "subject_refs": [
    {
      "kind": "file",
      "path": "src/parse.rs"
    }
  ],
  "committed": [
    "src/parse.rs"
  ]
}
